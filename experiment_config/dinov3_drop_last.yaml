backbone_arch: "dinov3_vitb16"

input_config:
  img_size: [256, 256]
  mean_std: "IMAGENET_MEAN_STD"

backbone_config:
  num_trainable_blocks: 2
  return_token: true
  norm_layer: true
  rope_sincos: true
  probing_from_layer: 10

max_epochs: 8
